/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['startup-template-sage.vercel.app'], // Agrega el dominio aquí
  },
};

module.exports = nextConfig;
